import Nav from "./Nav";

const styles = {
    h1: {
        fontSize: "3rem",
        color: "blue",
        textAlign: "center",
        padding: "1rem"
    },
};
const Header = () => {
    return (
        <header>
        <Nav />
        </header>
    );
    };

    export default Header;
